# Client environment variables
# Copy this to .env.local for local dev overrides

# Leave blank for local dev (Vite proxy handles /api)
# Set to your Railway backend URL for production builds
VITE_API_URL=

# Leave blank for local dev (falls back to ws://localhost:1234)
# Set to your Railway WebSocket URL for production builds
VITE_WS_URL=
